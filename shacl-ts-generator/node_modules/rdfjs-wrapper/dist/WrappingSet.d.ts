import type { IValueMapping } from "./type/IValueMapping.js";
import type { ITermMapping } from "./type/ITermMapping.js";
import { TermWrapper } from "./TermWrapper.js";
export declare class WrappingSet<T> implements Set<T> {
    private readonly subject;
    private readonly predicate;
    private readonly valueMapping;
    private readonly termMapping;
    constructor(subject: TermWrapper, predicate: string, valueMapping: IValueMapping<T>, termMapping: ITermMapping<T>);
    add(value: T): this;
    clear(): void;
    delete(value: T): boolean;
    forEach(cb: (item: T, index: T, set: Set<T>) => void, thisArg?: any): void;
    has(value: T): boolean;
    get size(): number;
    [Symbol.iterator](): SetIterator<T>;
    entries(): SetIterator<[T, T]>;
    keys(): SetIterator<T>;
    values(): SetIterator<T>;
    get [Symbol.toStringTag](): string;
    private quad;
    private get matches();
}
//# sourceMappingURL=WrappingSet.d.ts.map