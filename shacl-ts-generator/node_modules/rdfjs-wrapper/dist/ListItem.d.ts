import { TermWrapper } from "./TermWrapper.js";
import type { DataFactory, DatasetCore, Term } from "@rdfjs/types";
import type { IValueMapping } from "./type/IValueMapping";
import type { ITermMapping } from "./type/ITermMapping.js";
export declare class ListItem<T> extends TermWrapper {
    private readonly valueMapping;
    private readonly termMapping;
    constructor(term: Term, dataset: DatasetCore, factory: DataFactory, valueMapping: IValueMapping<T>, termMapping: ITermMapping<T>);
    get firstRaw(): Term | undefined;
    set firstRaw(value: Term | undefined);
    get restRaw(): Term | undefined;
    set restRaw(value: Term | undefined);
    get isListItem(): boolean;
    get isNil(): boolean;
    get first(): T;
    set first(value: T);
    get rest(): ListItem<T>;
    set rest(value: ListItem<T>);
    pop(): T;
    items(): Iterable<ListItem<T>>;
}
//# sourceMappingURL=ListItem.d.ts.map